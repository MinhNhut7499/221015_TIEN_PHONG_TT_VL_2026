# ============================================================
# Architecture AI Backend — Python dependencies
# Python 3.10+
# Install: pip install -r requirements.txt
# ============================================================

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.0

# ── Settings / environment ────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-dotenv==1.0.1

# ── Authentication ────────────────────────────────────────────
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
# Fernet symmetric encryption for provider API keys stored in the DB
# (app/security/secret_cipher.py). Already pulled in transitively by
# python-jose[cryptography]; pinned here to make the dependency explicit.
cryptography==42.0.8

# ── HTTP client (Google OAuth calls) ─────────────────────────
httpx==0.27.0

# ── Email (async SMTP — password-reset links) ─────────────────
aiosmtplib==3.0.1

# ── File upload (multipart/form-data parsing) ─────────────────
python-multipart==0.0.9

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ── Development utilities (optional but recommended) ──────────
# Uncomment when needed:
# black==24.4.2          # code formatter
# ruff==0.4.4            # fast linter
# mypy==1.10.0           # static type checker

# ── Database (SQL Server via SQLAlchemy async) ────────────────
sqlalchemy[asyncio]==2.0.30
aioodbc==0.5.2
pyodbc==5.1.0

# ── LLM SDKs ──────────────────────────────────────────────────
# Open-vocabulary pipeline: Gemini (Agent A + advocate/alt), OpenAI (arbiter),
# DeepSeek (OpenAI-compatible, via the openai SDK). Pillow reads image size +
# encodes JPEG for the vision calls. No torch / YOLO / CV deps — recognition is
# VLM + knowledge-base driven (add a style = add a KB row, no model training).
google-genai==1.75.0
openai==1.75.0
pillow==10.3.0

# ── Consensus measurement ─────────────────────────────────────
# Inter-judge agreement = mean pairwise Spearman rank correlation across the
# 3-judge panel (chatbot/utils/consensus.py). Pulls numpy as a transitive dep.
scipy==1.13.1
